#include <map>

class Solution {
	public:
		int singleNumber(int A[], int n) {
			map <int, int> numbers;
			for (int i = 0; i < n; i++)
				if map.find(i) 
					map.erase(i)
				else
					map[A[i]] = 1
		}
};
using namespace std;


int main() {
	return 0;
}
